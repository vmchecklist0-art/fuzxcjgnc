# dblenmersh

## Local setup

1. Copy `.env.example` to `.env.local`
2. Set `NEON_DATABASE_URL` and `IMGBB_API_KEY`
3. Run `npm install`
4. Start the app with `npm run dev`

For Vercel deployment, add `NEON_DATABASE_URL` and `IMGBB_API_KEY` as environment variables in your Vercel project.
